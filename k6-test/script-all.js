/*
 * Api Documentation
 * Api Documentation
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://github.com/OpenAPITools/openapi-generator
 *
 * OpenAPI generator version: 6.0.1-SNAPSHOT
 */


import http from "k6/http";
import { group, check, sleep } from "k6";

const BASE_URL = "http://145.108.225.17";
const ADMIN_BASIC_SERVICE_PORT = "18767";
const ASSURANCE_SERVICE_PORT = "18888";
const ADMIN_ORDER_SERVICE_PORT = "16112";
const ADMIN_ROUTE_SERVICE_PORT = "16113";
const ADMIN_TRAVEL_SERVICE_PORT = "16114";
const ADMIN_USER_SERVICE_PORT = "16115";
const BASIC_SERVICE_PORT = "15680";
const CANCEL_SERVICE_PORT = "18885";
const CONFIG_SERVICE_PORT = "15679";
const CONSIGN_PRICE_SERVICE_PORT = "16110";
const CONSIGN_SERVICE_PORT = "16111";
const CONTACTS_SERVICE_PORT = "12347";
const EXECUTE_SERVICE_PORT = "12386";
const FOOD_MAP_SERVICE_PORT = "18855";
const FOOD_SERVICE_PORT = "18856";
const INSIDE_PAYMENT_SERVICE_PORT = "18673";
const NOTIFICATION_SERVICE_PORT = "17853";
const ORDER_OTHER_SERVICE_PORT = "12032";
const ORDER_SERVICE_PORT = "12031";
const PAYMENT_SERVICE_PORT = "19001";
const PRESERVE_OTHER_SERVICE_PORT = "14569";
const PRESERVE_SERVICE_PORT = "14568";
const PRICE_SERVICE_PORT = "16579";
const REBOOK_SERVICE_PORT = "18886";
const ROUTE_PLAN_SERVICE_PORT = "14578";
const ROUTE_SERVICE_PORT = "11178";
const SEAT_SERVICE_PORT = "18898";
const SECURITY_SERVICE_PORT = "11188";
const STATION_SERVICE_PORT = "12345";
const TICKETINFO_SERVICE_PORT = "15681";
const TRAIN_SERVICE_PORT = "14567";
const TRAVEL2_SERVICE_PORT = "16346";
const TRAVEL_PLAN_SERVICE_PORT = "14322";
const TRAVEL_SERVICE_PORT = "12346";

// Sleep duration between successive requests.
// You might want to edit the value of this variable or remove calls to the sleep function on the script.
const SLEEP_DURATION = 0.1;
// Global variables should be initialized.
const TS_AUTH_SERVICE_URL = "http://145.108.225.17:12340";

const USERNAME_ADMIN = 'admin';
const PASSWORD_ADMIN = '222222';

const USERNAME = 'fdse_microservice';
const PASSWORD = '111111';

export function setup() {
    // authenticate via a Bearer token
    let params = {headers: {"Content-Type": "application/json", "Accept": "*/*"}};
    let bodyAdmin = {"username": USERNAME_ADMIN, "password": PASSWORD_ADMIN};
    const loginResAdmin = http.post(`${TS_AUTH_SERVICE_URL}/api/v1/users/login`, JSON.stringify(bodyAdmin), params);

    const authTokenAdmin = loginResAdmin.json('data')["token"];
    check(authTokenAdmin, { 'Logged in successfully': () => authTokenAdmin !== '' });
    let body = {"username": USERNAME, "password": PASSWORD};
    const loginRes = http.post(`${TS_AUTH_SERVICE_URL}/api/v1/users/login`, JSON.stringify(body), params);

    const authToken = loginRes.json('data')["token"];
    check(authToken, { 'Logged in successfully': () => authToken !== '' });

    return [authTokenAdmin, authToken];
}

export default function(data) {
    let params = {headers: {"Authorization": `Bearer ${data[0]}`, "Content-Type": "application/json", "Accept": "*/*"}};
    let params_nonAdmin = {headers: {"Authorization": `Bearer ${data[1]}`, "Content-Type": "application/json", "Accept": "*/*"}};

    // ts-admin-basic-info-service

    group("/api/v1/adminbasicservice/adminbasic/trains/{id}", () => {
        let id = 'GaoTieOne';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/trains/${id}`;

            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/prices", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/prices`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/prices`;
            let body = {"basicPriceRate": 0.38, "firstClassPriceRate": 1, "id": "6d20b8cb-039c-474c-ae25-b6177ea41152", "routeId": "92708982-77af-4318-be25-57ccb0ff69ad", "trainType": "GaoTieOne"};
            let request = http.del(url, JSON.stringify(body) , params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/configs/{name}", () => {
        let name = 'DirectTicketAllocationProportionFake';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/configs/${name}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/stations", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/stations`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/stations`;
            let body = {"id": "shanghai", "name": "Shang Hai", "stayTime": 10};
            let request = http.del(url, JSON.stringify(body) , params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/configs", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/configs`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/configs`;
            let body = {"description": "DirectTicketAllocationProportion", "name": "DirectTicketAllocationProportion", "value": "DirectTicketAllocationProportion value"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/trains", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/trains`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/trains`;
            let body = {"averageSpeed": 250, "confortClass": 200, "economyClass": 300, "id": "rapid-cfr"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/contacts", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/contacts`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/contacts`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "documentNumber": "54321", "documentType": 2, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "name": "MadaDinga", "phoneNumber": "74645"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminbasicservice/adminbasic/contacts/{contactsId}", () => {
        let contactsId = "153632a9-14eb-44f1-7b89-7431fea1cba3";

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_BASIC_SERVICE_PORT + `/api/v1/adminbasicservice/adminbasic/contacts/${contactsId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    // ts-assurance-service

    group("/api/v1/assuranceservice/assurance/orderid/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurance/orderid/${orderId}`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/assuranceservice/assurances/assuranceid/{assuranceId}", () => {
        let assuranceId = '153632a9-14eb-44f1-7b89-7431fea1cbbc';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/assuranceid/${assuranceId}`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/assuranceid/${assuranceId}`;
            let request = http.del(url, {}, params_nonAdmin);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });


    group("/api/v1/assuranceservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/welcome`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/assuranceservice/assurances/{typeIndex}/{orderId}", () => {
        let typeIndex = 1;
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/${typeIndex}/${orderId}`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/assuranceservice/assurances", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-admin-order-service

    group("/api/v1/adminorderservice/adminorder", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ORDER_SERVICE_PORT + `/api/v1/adminorderservice/adminorder`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_ORDER_SERVICE_PORT + `/api/v1/adminorderservice/adminorder`;
            let body = {
                "id": "d3c91694-d5b8-424c-9974-e14c89226e46",
                "boughtDate": 1656360258593,
                "travelDate": 1501257600000,
                "travelTime": 1367629200000,
                "accountId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "contactsName": "Contacts_One",
                "documentType": 1,
                "contactsDocumentNumber": "DocumentNumber_One",
                "trainNumber": "G1235",
                "coachNumber": 5,
                "seatClass": 2,
                "seatNumber": "FirstClass-29",
                "from": "shanghai",
                "to": "beijing",
                "status": 0,
                "price": "100.0"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminorderservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ORDER_SERVICE_PORT + `/api/v1/adminorderservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminorderservice/adminorder/{orderId}/{trainNumber}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let trainNumber = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ORDER_SERVICE_PORT + `/api/v1/adminorderservice/adminorder/${orderId}/${trainNumber}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    // ts-admin-route-service

    group("/api/v1/adminrouteservice/adminroute/{routeId}", () => {
        let routeId = 'ru123';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ROUTE_SERVICE_PORT + `/api/v1/adminrouteservice/adminroute/${routeId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/adminrouteservice/adminroute", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ROUTE_SERVICE_PORT + `/api/v1/adminrouteservice/adminroute`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_ROUTE_SERVICE_PORT + `/api/v1/adminrouteservice/adminroute`;
            let body = {
                "distanceList": "100",
                "endStation": "string",
                "id": "153632a9-14eb-44f1-7b89-7431fea1cba3",
                "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "startStation": "string",
                "stationList": "Hong Kong"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/adminrouteservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_ROUTE_SERVICE_PORT + `/api/v1/adminrouteservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-admin-travel-service

    group("/api/v1/admintravelservice/admintravel/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_TRAVEL_SERVICE_PORT + `/api/v1/admintravelservice/admintravel/${tripId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/admintravelservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_TRAVEL_SERVICE_PORT + `/api/v1/admintravelservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/admintravelservice/admintravel", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_TRAVEL_SERVICE_PORT + `/api/v1/admintravelservice/admintravel`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_TRAVEL_SERVICE_PORT + `/api/v1/admintravelservice/admintravel`;
            let body = {"endTime": "2022-06-28", "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f", "routeId": "92708982-77af-4318-be25-57ccb0ff69ad", "startingStationId": "string", "startingTime": "2022-06-28", "stationsId": "suzhou", "terminalStationId": "taiyuan", "trainTypeId": "GaoTieOne", "tripId": "D1345"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-admin-user-service

    group("/api/v1/adminuserservice/users/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_USER_SERVICE_PORT + `/api/v1/adminuserservice/users/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/adminuserservice/users/{userId}", () => {
        let userId = '153632a9-14eb-44f1-7b89-7431fea1cba1';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_USER_SERVICE_PORT + `/api/v1/adminuserservice/users/${userId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/adminuserservice/users", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ADMIN_USER_SERVICE_PORT + `/api/v1/adminuserservice/users`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ADMIN_USER_SERVICE_PORT + `/api/v1/adminuserservice/users`;
            let body = {"documentNum": "string", "documentType": 2, "email": "string", "gender": 2, "password": "string", "userName": "string"};

            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-assurance-service

    group("/api/v1/assuranceservice/assurances/types", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/types`;
            let request = http.get(url, params_nonAdmin);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/assuranceservice/assurances/{assuranceId}/{orderId}/{typeIndex}", () => {
        let typeIndex = 1;
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let assuranceId = '153632a9-14eb-44f1-7b89-7431fea1cbbc';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/${assuranceId}/${orderId}/${typeIndex}`;
            let request = http.patch(url, {}, params_nonAdmin);

            check(request, {
                "OK": (r) => r.status === 200
            });

        }
    });
    group("/api/v1/assuranceservice/assurances/orderid/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ASSURANCE_SERVICE_PORT + `/api/v1/assuranceservice/assurances/orderid/${orderId}`;
            let request = http.del(url, {}, params_nonAdmin);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    // ts-basic-service

    group("/api/v1/basicservice/basic/{stationName}", () => {
        let stationName = 'Mada\'s';

        // Request No. 1
        {
            let url = BASE_URL + `:` + BASIC_SERVICE_PORT + `/api/v1/basicservice/basic/${stationName}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/basicservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + BASIC_SERVICE_PORT + `/api/v1/basicservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/basicservice/basic/travel", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + BASIC_SERVICE_PORT + `/api/v1/basicservice/basic/travel`;
            let body = {"departureTime": "2022-06-28", "endPlace": "string", "startingPlace": "string", "trip": {"endTime": "2022-06-28", "routeId": "92708982-77af-4318-be25-57ccb0ff69ad", "startingStationId": "shanghai", "startingTime": "2022-06-28", "stationsId": "suzhou", "terminalStationId": "taiyuan", "trainTypeId": "GaoTieOne", "tripId": "153632a9-14eb-44f1-7b89-7431fea1cba2"}};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-cancel-service

    group("/api/v1/cancelservice/cancel/refound/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CANCEL_SERVICE_PORT + `/api/v1/cancelservice/cancel/refound/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/cancelservice/cancel/{orderId}/{loginId}", () => {
        let loginId = '4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f';
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CANCEL_SERVICE_PORT + `/api/v1/cancelservice/cancel/${orderId}/${loginId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/cancelservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CANCEL_SERVICE_PORT + `/api/v1/cancelservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-config-service

    group("/api/v1/configservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONFIG_SERVICE_PORT + `/api/v1/configservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/configservice/configs/{configName}", () => {
        let configName = 'sample-config';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONFIG_SERVICE_PORT + `/api/v1/configservice/configs/${configName}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + CONFIG_SERVICE_PORT + `/api/v1/configservice/configs/${configName}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });


    group("/api/v1/configservice/configs", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONFIG_SERVICE_PORT + `/api/v1/configservice/configs`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + CONFIG_SERVICE_PORT + `/api/v1/configservice/configs`;
            let body = {"description": "string", "name": "sample-config", "value": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 201
            });
        }
    });

    // ts-consign-price-service

    group("/api/v1/consignpriceservice/consignprice/{weight}/{isWithinRegion}", () => {
        let isWithinRegion = 'true'
        let weight = 100

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_PRICE_SERVICE_PORT + `/api/v1/consignpriceservice/consignprice/${weight}/${isWithinRegion}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignpriceservice/consignprice/price", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_PRICE_SERVICE_PORT + `/api/v1/consignpriceservice/consignprice/price`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/consignpriceservice/consignprice/config", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_PRICE_SERVICE_PORT + `/api/v1/consignpriceservice/consignprice/config`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignpriceservice/consignprice", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_PRICE_SERVICE_PORT + `/api/v1/consignpriceservice/consignprice`;
            let body = {"beyondPrice": 20000, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "index": 2, "initialPrice": 20000, "initialWeight": 20000, "withinPrice": 20000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignpriceservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_PRICE_SERVICE_PORT + `/api/v1/consignpriceservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-consign-service

    group("/api/v1/consignservice/consigns/{consignee}", () => {
        let consignee = 'Madalina';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/consigns/${consignee}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignservice/consigns", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/consigns`;
            let body = {
                "accountId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "handleDate": "2022-06-28",
                "targetDate": "2022-06-28 11:56:57",
                "from": "Shang Hai",
                "to": "Tai Yuan",
                "orderId": "343cfd59-bff1-4199-b530-6ec79758bcbc",
                "consignee": "Madalina",
                "phone": "12082",
                "weight": "50",
                "id": "",
                "isWithin": false
            }
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "PUT OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/consigns`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "consignee": "string", "from": "string", "handleDate": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "orderId": "153632a9-14eb-44f1-7b89-7431fea1cbde", "phone": "string", "targetDate": "string", "to": "string", "weight": 20000, "within": true};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignservice/consigns/account/{id}", () => {
        let id = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/consigns/account/${id}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignservice/consigns/order/{id}", () => {
        let id = '153632a9-14eb-44f1-7b89-7431fea1cbde';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/consigns/order/${id}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/consignservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONSIGN_SERVICE_PORT + `/api/v1/consignservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-contacts-service

    group("/api/v1/contactservice/contacts/{contactsId}", () => {
        let contactsId = "153632a9-14eb-44f1-7b89-7431fea1cba3";

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts/${contactsId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/contactservice/contacts", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "documentNumber": "string", "documentType": 2, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "name": "string", "phoneNumber": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 201
            });
        }
    });


    group("/api/v1/contactservice/contacts/admin", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts/admin`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "documentNumber": "string", "documentType": 2, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "name": "string", "phoneNumber": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/contactservice/contacts/{id}", () => {
        let id = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts/${id}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/contactservice/contacts/account/{accountId}", () => {
        let accountId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts/account/${accountId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/contactservice/contacts/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + CONTACTS_SERVICE_PORT + `/api/v1/contactservice/contacts/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-execute-service

    group("/api/v1/executeservice/execute/execute/{orderId}", () => {
        let orderId = '5ad7750b-a68b-49c0-a8c0-32776b067703';

        // Request No. 1
        {
            let url = BASE_URL + `:` + EXECUTE_SERVICE_PORT + `/api/v1/executeservice/execute/execute/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/executeservice/execute/collected/{orderId}", () => {
        let orderId = '5ad7750b-a68b-49c0-a8c0-32776b067703';

        // Request No. 1
        {
            let url = BASE_URL + `:` + EXECUTE_SERVICE_PORT + `/api/v1/executeservice/execute/collected/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/executeservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + EXECUTE_SERVICE_PORT + `/api/v1/executeservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-food-map-service

    group("/api/v1/foodmapservice/trainfoods/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/trainfoods/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/foodmapservice/foodstores", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/foodstores`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/foodstores`;
            let body = ["cluj napoca", "bucuresti nord"]
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/foodmapservice/trainfoods", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/trainfoods`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/foodmapservice/foodstores/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/foodstores/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/foodmapservice/trainfoods/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/trainfoods/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/foodmapservice/foodstores/{stationId}", () => {
        let stationId = 'shanghai';

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_MAP_SERVICE_PORT + `/api/v1/foodmapservice/foodstores/${stationId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-food-service

    group("/api/v1/foodservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/foodservice/orders/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/orders/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/orders/${orderId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/foodservice/orders", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/orders`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/orders`;
            let body = {"foodName": "string", "foodType": 2, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703", "price": 20000, "stationName": "string", "storeName": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/foodservice/foods/{date}/{startStation}/{endStation}/{tripId}", () => {
        let date = '2022-06-30';
        let endStation = 'Shang Hai';
        let startStation = 'Su Zhou';
        let tripId = 'D1345';

        // Request No. 1
        {
            let url = BASE_URL + `:` + FOOD_SERVICE_PORT + `/api/v1/foodservice/foods/${date}/${startStation}/${endStation}/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-inside-payment-service

    group("/api/v1/inside_pay_service/inside_payment/money", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/money`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/inside_pay_service/inside_payment/drawback/{userId}/{money}", () => {
        let money = 1000
        let userId = '153632a9-14eb-44f1-7b89-7431fea1cba1';

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/drawback/${userId}/${money}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inside_pay_service/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inside_pay_service/inside_payment/{userId}/{money}", () => {
        let money = '100';
        let userId = '153632a9-14eb-44f1-7b89-7431fea1cba1';

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/${userId}/${money}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/inside_pay_service/inside_payment/account", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/account`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/account`;
            let body = {"money": "string", "userId": "153632a9-14eb-44f1-7b89-7431fea1cba1"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inside_pay_service/inside_payment/payment", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/payment`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inside_pay_service/inside_payment", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment`;
            let body = {
                "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703",
                "tripId": "G1237"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inside_pay_service/inside_payment/difference", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + INSIDE_PAYMENT_SERVICE_PORT + `/api/v1/inside_pay_service/inside_payment/difference`;
            let body = {"orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703", "price": "100", "tripId": "D1345", "userId": "153632a9-14eb-44f1-7b89-7431fea1cba1"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-notification-service

    group("/api/v1/notifyservice/notification/order_cancel_success", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + NOTIFICATION_SERVICE_PORT + `/api/v1/notifyservice/notification/order_cancel_success`;
            let body = {"date": 1656360258593, "email": "string", "endPlace": "string", "orderNumber": "string", "price": "string", "seatClass": "string", "seatNumber": "string", "startingPlace": "string", "startingTime": "string", "username": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifyservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + NOTIFICATION_SERVICE_PORT + `/api/v1/notifyservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifyservice/notification/order_create_success", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + NOTIFICATION_SERVICE_PORT + `/api/v1/notifyservice/notification/order_create_success`;
            let body = {"date": 1656360258593, "email": "string", "endPlace": "string", "orderNumber": "string", "price": "string", "seatClass": "string", "seatNumber": "string", "startingPlace": "string", "startingTime": "string", "username": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifyservice/notification/order_changed_success", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + NOTIFICATION_SERVICE_PORT + `/api/v1/notifyservice/notification/order_changed_success`;
            let body = {"date": 1656360258593, "email": "string", "endPlace": "string", "orderNumber": "string", "price": "string", "seatClass": "string", "seatNumber": "string", "startingPlace": "string", "startingTime": "string", "username": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifyservice/notification/preserve_success", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + NOTIFICATION_SERVICE_PORT + `/api/v1/notifyservice/notification/preserve_success`;
            let body = {"date": 1656360258593, "email": "string", "endPlace": "string", "orderNumber": "string", "price": "string", "seatClass": "string", "seatNumber": "string", "startingPlace": "string", "startingTime": "string", "username": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-order-other-service

    group("/api/v1/orderOtherService/orderOther/orderPay/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/orderPay/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/tickets", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/tickets`;
            let body = {"destStation": "string", "seatType": 2, "startStation": "string", "trainNumber": "G1235", "travelDate": 1501257600000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/orderOtherService/orderOther/status/{orderId}/{status}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let status = 1

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/status/${orderId}/${status}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/${orderId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/orderOtherService/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/refresh", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/refresh`;
            let body = {
                "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "enableStateQuery": false,
                "enableTravelDateQuery": false,
                "enableBoughtDateQuery": false,
                "travelDateStart": null,
                "travelDateEnd": null,
                "boughtDateStart": null,
                "boughtDateEnd": null
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/security/{checkDate}/{accountId}", () => {
        let accountId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let checkDate = new Date();

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/security/${checkDate}/${accountId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/query", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/query`;
            let body = {"boughtDateEnd": "2022-06-28", "boughtDateStart": "2022-06-28", "enableBoughtDateQuery": true, "enableStateQuery": true, "enableTravelDateQuery": true, "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f", "state": 2, "travelDateEnd": "2022-06-28", "travelDateStart": "2022-06-28"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/{travelDate}/{trainNumber}", () => {
        let travelDate = new Date();
        let trainNumber = 'G1235';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/${travelDate}/${trainNumber}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/admin", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/admin`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "PUT OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/admin`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderOtherService/orderOther/price/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_OTHER_SERVICE_PORT + `/api/v1/orderOtherService/orderOther/price/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-order-service

    group("/api/v1/orderservice/order/tickets", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/tickets`;
            let body = {"destStation": "", "seatType": 0, "startStation": "", "trainNumber": "", "travelDate": 1501257600000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/orderservice/order/admin", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/admin`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/admin`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/order/query", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/query`;
            let body = {"boughtDateEnd": "2022-06-28", "boughtDateStart": "2022-06-28", "enableBoughtDateQuery": true, "enableStateQuery": true, "enableTravelDateQuery": true, "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f", "state": 2, "travelDateEnd": "2022-06-28", "travelDateStart": "2022-06-28"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/order/{travelDate}/{trainNumber}", () => {
        let travelDate = new Date();
        let trainNumber = 'G1235';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/${travelDate}/${trainNumber}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/orderservice/order/status/{orderId}/{status}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let status = 1;

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/status/${orderId}/${status}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/order", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order`;
            let body = {"accountId": "153632a9-14eb-44f1-7b89-7431fea1cba3", "boughtDate": "2022-06-28", "coachNumber": 2, "contactsDocumentNumber": "string", "contactsName": "string", "documentType": 2, "from": "string", "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "price": "string", "seatClass": 2, "seatNumber": "string", "status": 2, "to": "string", "trainNumber": "G1235", "travelDate": 1501257600000, "travelTime": "1367629200000"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/order/refresh", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/refresh`;
            let body = {
                "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "enableStateQuery": false,
                "enableTravelDateQuery": false,
                "enableBoughtDateQuery": false,
                "travelDateStart": null,
                "travelDateEnd": null,
                "boughtDateStart": null,
                "boughtDateEnd": null
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/order/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/${orderId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/orderservice/order/security/{checkDate}/{accountId}", () => {
        let accountId = '153632a9-14eb-44f1-7b89-7431fea1cba3';
        let checkDate = new Date();

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/security/${checkDate}/${accountId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/orderservice/order/orderPay/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/orderPay/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orderservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/orderservice/order/price/{orderId}", () => {
        let orderId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ORDER_SERVICE_PORT + `/api/v1/orderservice/order/price/${orderId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-payment-service

    group("/api/v1/paymentservice/payment/money", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PAYMENT_SERVICE_PORT + `/api/v1/paymentservice/payment/money`;
            let body = {"id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703", "price": "string", "userId": "153632a9-14eb-44f1-7b89-7431fea1cba1"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/paymentservice/payment", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PAYMENT_SERVICE_PORT + `/api/v1/paymentservice/payment`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + PAYMENT_SERVICE_PORT + `/api/v1/paymentservice/payment`;
            let body = {"id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703", "price": "string", "userId": "153632a9-14eb-44f1-7b89-7431fea1cba1"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/paymentservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PAYMENT_SERVICE_PORT + `/api/v1/paymentservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-preserve-other-service

    group("/api/v1/preserveotherservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRESERVE_OTHER_SERVICE_PORT + `/api/v1/preserveotherservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/preserveotherservice/preserveOther", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRESERVE_OTHER_SERVICE_PORT + `/api/v1/preserveotherservice/preserveOther`;
            let body ={
                "accountId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "contactsId": "8cd5070f-a5a5-4d82-a956-b8b98a10ad01",
                "tripId": "D1345",
                "seatType": "2",
                "date": "2022-06-30",
                "from": "Shang Hai",
                "to": "Su Zhou",
                "assurance": "0",
                "foodType": 1,
                "foodName": "Bone Soup",
                "foodPrice": 2.5,
                "stationName": "",
                "storeName": ""
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-preserve-service

    group("/api/v1/preserveservice/preserve", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRESERVE_SERVICE_PORT + `/api/v1/preserveservice/preserve`;
            let body = {
                "accountId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "contactsId": "8cd5070f-a5a5-4d82-a956-b8b98a10ad01",
                "tripId": "D1345",
                "seatType": "2",
                "date": "2022-06-30",
                "from": "Shang Hai",
                "to": "Su Zhou",
                "assurance": "0",
                "foodType": 1,
                "foodName": "Bone Soup",
                "foodPrice": 2.5,
                "stationName": "",
                "storeName": ""
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/preserveservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRESERVE_SERVICE_PORT + `/api/v1/preserveservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-price-service

    group("/api/v1/priceservice/prices/{routeId}/{trainType}", () => {
        let routeId = 'ru102';
        let trainType = 'GaoTieOne';

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRICE_SERVICE_PORT + `/api/v1/priceservice/prices/${routeId}/${trainType}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/priceservice/prices", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRICE_SERVICE_PORT + `/api/v1/priceservice/prices`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + PRICE_SERVICE_PORT + `/api/v1/priceservice/prices`;
            let body = {"basicPriceRate": 20000, "firstClassPriceRate": 20000, "id": "153632a9-14eb-44f1-7b89-7431fea1cba3", "routeId": "92708982-77af-4318-be25-57ccb0ff69ad", "trainType": "string"};
            let request = http.del(url, JSON.stringify(body) , params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/priceservice/prices/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + PRICE_SERVICE_PORT + `/api/v1/priceservice/prices/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-rebook-service

    group("/api/v1/rebookservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + REBOOK_SERVICE_PORT + `/api/v1/rebookservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/rebookservice/rebook/difference", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + REBOOK_SERVICE_PORT + `/api/v1/rebookservice/rebook/difference`;
            let body = {
                "date": "2022-06-28",
                "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "oldTripId": "D1345",
                "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703",
                "seatType": "2",
                "tripId": "D1345"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/rebookservice/rebook", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + REBOOK_SERVICE_PORT + `/api/v1/rebookservice/rebook`;
            let body = {
                "date": "2022-06-28",
                "loginId": "4d2a46c7-71cb-4cf1-b5bb-b68406d9da6f",
                "oldTripId": "string",
                "orderId": "5ad7750b-a68b-49c0-a8c0-32776b067703",
                "seatType": 2,
                "tripId": "D1345"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-route-plan-service

    group("/api/v1/routeplanservice/routePlan/quickestRoute", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_PLAN_SERVICE_PORT + `/api/v1/routeplanservice/routePlan/quickestRoute`;
            let body = {"formStationName": "Nan Jing", "num": 2, "toStationName": "Shang Hai", "travelDate": 1501257600000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/routeplanservice/routePlan/minStopStations", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_PLAN_SERVICE_PORT + `/api/v1/routeplanservice/routePlan/minStopStations`;
            let body = {"formStationName": "Nan Jing", "num": 2, "toStationName": "Shang Hai", "travelDate": 1501257600000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/routeplanservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_PLAN_SERVICE_PORT + `/api/v1/routeplanservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/routeplanservice/routePlan/cheapestRoute", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_PLAN_SERVICE_PORT + `/api/v1/routeplanservice/routePlan/cheapestRoute`;
            let body = {"formStationName": "Nan Jing", "num": 2, "toStationName": "Shang Hai", "travelDate": 1501257600000};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-route-service

    group("/api/v1/routeservice/routes", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/routes`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/routes`;
            let body = {
                "distanceList": "100",
                "endStation": "string",
                "id": "153632a9-14eb-44f1-7b89-7431fea1cba3",
                "startStation": "string",
                "stationList": "Hong Kong"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/routeservice/routes/{startId}/{terminalId}", () => {
        let startId = '123';
        let terminalId = '234';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/routes/${startId}/${terminalId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/routeservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });


    group("/api/v1/routeservice/routes/{routeId}", () => {
        let routeId = '123';

        // Request No. 1
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/routes/${routeId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + ROUTE_SERVICE_PORT + `/api/v1/routeservice/routes/${routeId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    // ts-seat-service

    group("/api/v1/seatservice/seats", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + SEAT_SERVICE_PORT + `/api/v1/seatservice/seats`;
            let body = {"destStation": "", "seatType": 2, "startStation": "", "trainNumber": "G1235", "travelDate": "28-06-2022"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/seatservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + SEAT_SERVICE_PORT + `/api/v1/seatservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/seatservice/seats/left_tickets", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + SEAT_SERVICE_PORT + `/api/v1/seatservice/seats/left_tickets`;
            let body = {"destStation": "", "seatType": 2, "startStation": "", "trainNumber": "G1235", "travelDate": "28-06-2022"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-security-service
    group("/api/v1/securityservice/securityConfigs", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + SECURITY_SERVICE_PORT + `/api/v1/securityservice/securityConfigs`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + SECURITY_SERVICE_PORT + `/api/v1/securityservice/securityConfigs`;
            let body = {
                "description": "string",
                "id": "153632a9-14eb-44f1-7b89-7431fea1cba3",
                "name": "string",
                "value": "string"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/securityservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + SECURITY_SERVICE_PORT + `/api/v1/securityservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/securityservice/securityConfigs/{accountId}", () => {
        let accountId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + SECURITY_SERVICE_PORT + `/api/v1/securityservice/securityConfigs/${accountId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/securityservice/securityConfigs/{id}", () => {
        let id = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + SECURITY_SERVICE_PORT + `/api/v1/securityservice/securityConfigs/${id}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    // ts-station-service

    group("/api/v1/stationservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + STATION_SERVICE_PORT + `/api/v1/stationservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "Welcome OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stationservice/stations/id/{stationNameForId}", () => {
        let stationNameForId = 'taiyuan';

        // Request No. 1
        {
            let url = BASE_URL + `:` + STATION_SERVICE_PORT + `/api/v1/stationservice/stations/id/${stationNameForId}`;
            let request = http.get(url, params);

            check(request, {
                "Station was retrieved correctly": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stationservice/stations", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + STATION_SERVICE_PORT + `/api/v1/stationservice/stations`;
            let request = http.get(url, params);

            check(request, {
                "Stations were retrieved correctly": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + STATION_SERVICE_PORT + `/api/v1/stationservice/stations`;
            let body = {"id": "hangzhou", "name": "Hang Zhou", "stayTime": 10};
            let request = http.del(url, JSON.stringify(body), params);

            const isSuccessfulDelete = check(null, {
                'Station was deleted correctly': () => request.status === 200,
            });
        }
    });

    // ts-ticketinfo-service

    group("/api/v1/ticketinfoservice/ticketinfo/{name}", () => {
        let name = 'sample-name';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TICKETINFO_SERVICE_PORT + `/api/v1/ticketinfoservice/ticketinfo/${name}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/ticketinfoservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TICKETINFO_SERVICE_PORT + `/api/v1/ticketinfoservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/ticketinfoservice/ticketinfo", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TICKETINFO_SERVICE_PORT + `/api/v1/ticketinfoservice/ticketinfo`;
            let body = {
                "departureTime": 1367629200000,
                "endPlace": "Hong Kong",
                "startingPlace": "Shanghai",
                "trip": {
                    "endTime": "2022-06-28",
                    "routeId": "92708982-77af-4318-be25-57ccb0ff69ad",
                    "startingStationId": "shanghai",
                    "startingTime": "2022-06-28",
                    "stationsId": "suzhou",
                    "terminalStationId": "taiyuan",
                    "trainTypeId": "GaoTieOne",
                    "tripId": "153632a9-14eb-44f1-7b89-7431fea1cba2"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-train-service

    group("/api/v1/trainservice/trains/{id}", () => {
        let id = 'GL22';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAIN_SERVICE_PORT + `/api/v1/trainservice/trains/${id}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAIN_SERVICE_PORT + `/api/v1/trainservice/trains/${id}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/trainservice/trains", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAIN_SERVICE_PORT + `/api/v1/trainservice/trains`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAIN_SERVICE_PORT + `/api/v1/trainservice/trains`;
            let body = {
                "averageSpeed": 2,
                "confortClass": 2,
                "economyClass": 2,
                "id": "153632a9-14eb-44f1-7b89-7431fea1cba3"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/trainservice/trains/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAIN_SERVICE_PORT + `/api/v1/trainservice/trains/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    // ts-travel2-service

    group("/api/v1/travel2service/routes/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/routes/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/trips/routes", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips/routes`;
            let body = ["shanghai"]
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/trips/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips/${tripId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/travel2service/admin_trip", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/admin_trip`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/train_types/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/train_types/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/trip_detail", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trip_detail`;
            let body = {"from": "string", "to": "string", "travelDate": 1501257600000, "tripId": "D1345"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/trips/left", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips/left`;
            let body = {"departureTime": "2022-06-28", "endPlace": "string", "startingPlace": "string"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travel2service/trips", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAVEL2_SERVICE_PORT + `/api/v1/travel2service/trips`;
            let body = {
                "endTime": "2022-06-28",
                "routeId": "92708982-77af-4318-be25-57ccb0ff69ad",
                "startingStationId": "suzhou",
                "startingTime": "2022-06-28",
                "stationsId": "suzhou",
                "terminalStationId": "taiyuan",
                "trainTypeId": "GaoTieOne",
                "tripId": "D1345"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-travel-plan-service

    group("/api/v1/travelplanservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_PLAN_SERVICE_PORT + `/api/v1/travelplanservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelplanservice/travelPlan/transferResult", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_PLAN_SERVICE_PORT + `/api/v1/travelplanservice/travelPlan/transferResult`;
            let body = {
                "fromStationName": "string",
                "toStationName": "string",
                "trainType": "string",
                "travelDate": 1501257600000,
                "viaStationName": "string"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelplanservice/travelPlan/quickest", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_PLAN_SERVICE_PORT + `/api/v1/travelplanservice/travelPlan/quickest`;
            let body = {
                "startingPlace": "Nan Jing",
                "endPlace": "Shang Hai",
                "departureTime": "2022-06-28"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelplanservice/travelPlan/cheapest", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_PLAN_SERVICE_PORT + `/api/v1/travelplanservice/travelPlan/cheapest`;
            let body = {
                "startingPlace": "Nan Jing",
                "endPlace": "Shang Hai",
                "departureTime": "2022-06-28"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelplanservice/travelPlan/minStation", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_PLAN_SERVICE_PORT + `/api/v1/travelplanservice/travelPlan/minStation`;
            let body = {
                "startingPlace": "Nan Jing",
                "endPlace": "Shang Hai",
                "departureTime": "2022-06-28"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    // ts-travel-service

    group("/api/v1/travelservice/trip_detail", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trip_detail`;
            let body = {"from": "string", "to": "string", "travelDate": 1501257600000, "tripId": "153632a9-14eb-44f1-7b89-7431fea1cba3"};
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/routes/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3';

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/routes/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/trips", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips`;
            let body = {
                "endTime": "2022-06-28",
                "routeId": "92708982-77af-4318-be25-57ccb0ff69ad",
                "startingStationId": "shanghai",
                "startingTime": "2022-06-28",
                "stationsId": "suzhou",
                "terminalStationId": "taiyuan",
                "trainTypeId": "GaoTieOne",
                "tripId": "153632a9-14eb-44f1-7b89-7431fea1cba2"
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/trips/routes", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips/routes`;
            let body = ["0b23bd3e-876a-4af3-b920-c50a90c90b04"]
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/train_types/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba3'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/train_types/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/trips/{tripId}", () => {
        let tripId = '153632a9-14eb-44f1-7b89-7431fea1cba2'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips/${tripId}`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips/${tripId}`;
            let request = http.del(url, {}, params);

            check(request, {
                "DELETE OK": (r) => r.status === 200
            });

        }
    });

    group("/api/v1/travelservice/welcome", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/welcome`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/trips/left", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/trips/left`;
            let body = {
                "startingPlace": "Nan Jing",
                "endPlace": "Shang Hai Hong Qiao",
                "departureTime": "2022-07-28"
            }
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "POST OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/travelservice/admin_trip", () => {

        // Request No. 1
        {
            let url = BASE_URL + `:` + TRAVEL_SERVICE_PORT + `/api/v1/travelservice/admin_trip`;
            let request = http.get(url, params);

            check(request, {
                "GET OK": (r) => r.status === 200
            });
        }
    });
}
